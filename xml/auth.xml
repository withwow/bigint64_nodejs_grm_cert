<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="auth">  

  <select id="appversion">
    SELECT 
      appversion,
      reviewversion
    FROM
      version
    WHERE
      os = #{os}
  </select>

  <select id="login">
    SELECT 
      uid,
      pf,
      nickname,
      skip_tutorial,
      accesscode
    FROM
      member
    WHERE
      uid = #{uid}
  </select>

  <insert id="hist_login">
    INSERT INTO hist_login
    (
      uid,
      regdate
    )
    VALUES
    (
      #{uid},
      now()
    )
  </insert>

</mapper>